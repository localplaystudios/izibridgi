local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local plr = Players.LocalPlayer

local gui = Instance.new("ScreenGui", game.CoreGui)
gui.Name = "EpanniyFling"..math.random(1,9999999)

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0, 340, 0, 260)
frame.Position = UDim2.new(0.3, 0, 0.3, 0)
frame.BackgroundColor3 = Color3.fromRGB(20, 10, 30)
frame.Active = true
frame.Draggable = true
local frameCorner = Instance.new("UICorner", frame)
frameCorner.CornerRadius = UDim.new(0, 14)

-- Градиент
local gradient = Instance.new("UIGradient", frame)
gradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(60, 20, 80)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(25, 5, 40)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 5, 20))
})
gradient.Rotation = 45

local titleFrame = Instance.new("Frame", gui)
titleFrame.Size = UDim2.new(0, 340, 0, 35)
titleFrame.Position = UDim2.new(0.3, 0, 0.3, 0)
titleFrame.BackgroundColor3 = Color3.fromRGB(60, 20, 80)
titleFrame.BackgroundTransparency = 0
titleFrame.Visible = true
titleFrame.Active = true
titleFrame.Draggable = true
local titleCorner = Instance.new("UICorner", titleFrame)
titleCorner.CornerRadius = UDim.new(0, 14)

local titleGrad = Instance.new("UIGradient", titleFrame)
titleGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(80, 30, 120)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(40, 10, 70))
})
titleGrad.Rotation = 90

local title = Instance.new("TextLabel", titleFrame)
title.Size = UDim2.new(1, 0, 1, 0)
title.Position = UDim2.new(0, 0, 0, 0)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 18
title.Text = "🤙 EPANNIY FLING"
title.TextColor3 = Color3.fromRGB(255, 255, 255)

-- Кнопка свернуть (на заголовке)
local minimizeBtn = Instance.new("TextButton", titleFrame)
minimizeBtn.Size = UDim2.new(0, 30, 0, 30)
minimizeBtn.Position = UDim2.new(1, -35, 0, 2)
minimizeBtn.BackgroundColor3 = Color3.fromRGB(40, 10, 60)
minimizeBtn.Text = "─"
minimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
minimizeBtn.TextSize = 20
minimizeBtn.Font = Enum.Font.GothamBold
local minCorner = Instance.new("UICorner", minimizeBtn)
minCorner.CornerRadius = UDim.new(0, 8)

local isMinimized = false

minimizeBtn.MouseButton1Click:Connect(function()
    isMinimized = not isMinimized
    frame.Visible = not isMinimized
    titleFrame.Size = isMinimized and UDim2.new(0, 340, 0, 35) or UDim2.new(0, 340, 0, 35)
    minimizeBtn.Text = isMinimized and "+" or "─"
end)

-- Контент фрейма
local contentFrame = Instance.new("Frame", frame)
contentFrame.Size = UDim2.new(1, 0, 1, -35)
contentFrame.Position = UDim2.new(0, 0, 0, 35)
contentFrame.BackgroundTransparency = 1

-- Player name textbox
local nameBox = Instance.new("TextBox", contentFrame)
nameBox.Size = UDim2.new(0.62, 0, 0, 30)
nameBox.Position = UDim2.new(0.05, 0, 0, 10)
nameBox.PlaceholderText = "Player name (full or partial)"
nameBox.ClearTextOnFocus = false
nameBox.Font = Enum.Font.Gotham
nameBox.TextSize = 15
nameBox.TextColor3 = Color3.new(1,1,1)
nameBox.BackgroundColor3 = Color3.fromRGB(40, 20, 50)
nameBox.Text = ""
local nameCorner = Instance.new("UICorner", nameBox)
nameCorner.CornerRadius = UDim.new(0, 8)

-- Dropdown button
local dropdownBtn = Instance.new("TextButton", contentFrame)
dropdownBtn.Size = UDim2.new(0.28, 0, 0, 30)
dropdownBtn.Position = UDim2.new(0.69, 0, 0, 10)
dropdownBtn.Text = "List ▼"
dropdownBtn.Font = Enum.Font.Gotham
dropdownBtn.TextSize = 15
dropdownBtn.TextColor3 = Color3.new(1,1,1)
dropdownBtn.BackgroundColor3 = Color3.fromRGB(50, 25, 65)
local dropCorner = Instance.new("UICorner", dropdownBtn)
dropCorner.CornerRadius = UDim.new(0, 8)

-- Dropdown container
local dropdownContainer = Instance.new("ScrollingFrame", contentFrame)
dropdownContainer.Size = UDim2.new(0.92, 0, 0, 100)
dropdownContainer.Position = UDim2.new(0.04, 0, 0, 45)
dropdownContainer.BackgroundColor3 = Color3.fromRGB(30, 15, 40)
dropdownContainer.BorderSizePixel = 0
dropdownContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
dropdownContainer.ScrollBarThickness = 6
dropdownContainer.Visible = false
local dropContCorner = Instance.new("UICorner", dropdownContainer)
dropContCorner.CornerRadius = UDim.new(0, 8)

local dropdownButtons = {}

local function updateDropdown()
    for _, btn in pairs(dropdownButtons) do btn:Destroy() end
    dropdownButtons = {}
    local yPos = 0
    local players = Players:GetPlayers()
    for i, p in ipairs(players) do
        if p ~= plr then
            local btn = Instance.new("TextButton", dropdownContainer)
            btn.Size = UDim2.new(1, -10, 0, 25)
            btn.Position = UDim2.new(0, 5, 0, yPos)
            btn.BackgroundColor3 = Color3.fromRGB(45, 25, 55)
            btn.Font = Enum.Font.Gotham
            btn.TextSize = 14
            btn.Text = p.Name
            btn.TextColor3 = Color3.new(1,1,1)
            local btnCorner = Instance.new("UICorner", btn)
            btnCorner.CornerRadius = UDim.new(0, 6)
            btn.AutoButtonColor = true
            btn.MouseButton1Click:Connect(function()
                nameBox.Text = p.Name
                dropdownContainer.Visible = false
            end)
            yPos = yPos + 30
            table.insert(dropdownButtons, btn)
        end
    end
    dropdownContainer.CanvasSize = UDim2.new(0, 0, 0, yPos)
end

spawn(function()
    while true do
        updateDropdown()
        task.wait(5)
    end
end)

dropdownBtn.MouseButton1Click:Connect(function()
    dropdownContainer.Visible = not dropdownContainer.Visible
end)

-- Fling variables
local flingSpin = nil
local loopFling = false

local function startFling()
    if flingSpin then flingSpin:Destroy() end
    local char = plr.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    flingSpin = Instance.new("BodyAngularVelocity")
    flingSpin.AngularVelocity = Vector3.new(0, 999999, 0)
    flingSpin.MaxTorque = Vector3.new(999999, 999999, 999999)
    flingSpin.P = 10000
    flingSpin.Parent = hrp
end

local function stopFling()
    if flingSpin then
        flingSpin:Destroy()
        flingSpin = nil
    end
end

-- Fling Once button
local flingBtn = Instance.new("TextButton", contentFrame)
flingBtn.Size = UDim2.new(0.92, 0, 0, 38)
flingBtn.Position = UDim2.new(0.04, 0, 0, 195)
flingBtn.Text = "▶ FLING ONCE"
flingBtn.Font = Enum.Font.GothamBold
flingBtn.TextSize = 16
flingBtn.BackgroundColor3 = Color3.fromRGB(80, 30, 110)
flingBtn.TextColor3 = Color3.new(1,1,1)
local flingCorner = Instance.new("UICorner", flingBtn)
flingCorner.CornerRadius = UDim.new(0, 10)

-- Loop Fling toggle
local loopBtn = Instance.new("TextButton", contentFrame)
loopBtn.Size = UDim2.new(0.4, 0, 0, 38)
loopBtn.Position = UDim2.new(0.05, 0, 0, 150)
loopBtn.Text = "🔁 LOOP: OFF"
loopBtn.Font = Enum.Font.GothamBold
loopBtn.TextSize = 14
loopBtn.BackgroundColor3 = Color3.fromRGB(50, 20, 70)
loopBtn.TextColor3 = Color3.new(1,1,1)
local loopCorner = Instance.new("UICorner", loopBtn)
loopCorner.CornerRadius = UDim.new(0, 10)

loopBtn.MouseButton1Click:Connect(function()
    loopFling = not loopFling
    loopBtn.Text = loopFling and "🔁 LOOP: ON" or "🔁 LOOP: OFF"
    loopBtn.BackgroundColor3 = loopFling and Color3.fromRGB(100, 40, 140) or Color3.fromRGB(50, 20, 70)
    if not loopFling then stopFling() end
end)

-- Destroy GUI button
local destroyBtn = Instance.new("TextButton", contentFrame)
destroyBtn.Size = UDim2.new(0.4, 0, 0, 38)
destroyBtn.Position = UDim2.new(0.55, 0, 0, 150)
destroyBtn.Text = "✕ DESTROY"
destroyBtn.Font = Enum.Font.GothamBold
destroyBtn.TextSize = 14
destroyBtn.BackgroundColor3 = Color3.fromRGB(120, 30, 40)
destroyBtn.TextColor3 = Color3.new(1,1,1)
local destCorner = Instance.new("UICorner", destroyBtn)
destCorner.CornerRadius = UDim.new(0, 10)

destroyBtn.MouseButton1Click:Connect(function()
    gui:Destroy()
end)

-- Noclip toggle button
local noclipBtn = Instance.new("TextButton", contentFrame)
noclipBtn.Size = UDim2.new(0.92, 0, 0, 30)
noclipBtn.Position = UDim2.new(0.04, 0, 0, 110)
noclipBtn.Text = "🚪 NOCLIP: OFF [Press E]"
noclipBtn.Font = Enum.Font.GothamBold
noclipBtn.TextSize = 14
noclipBtn.BackgroundColor3 = Color3.fromRGB(40, 20, 60)
noclipBtn.TextColor3 = Color3.new(1,1,1)
local nocCorner = Instance.new("UICorner", noclipBtn)
nocCorner.CornerRadius = UDim.new(0, 10)

local noclipEnabled = false

noclipBtn.MouseButton1Click:Connect(function()
    noclipEnabled = not noclipEnabled
    noclipBtn.Text = noclipEnabled and "🚶‍♂️ NOCLIP: ON" or "🚶‍♂️ NOCLIP: OFF"
    noclipBtn.BackgroundColor3 = noclipEnabled and Color3.fromRGB(80, 30, 120) or Color3.fromRGB(40, 20, 60)
end)

-- Find target function
local function findTarget(name)
    local nameLower = name:lower()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= plr and p.Name:lower():sub(1, #nameLower) == nameLower then
            return p
        end
    end
    return nil
end

-- Fling Once action
flingBtn.MouseButton1Click:Connect(function()
    local target = findTarget(nameBox.Text)
    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
        local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = target.Character.HumanoidRootPart.CFrame + Vector3.new(2, 0, 0)
            startFling()
        end
    end
end)

-- Loop Fling logic
RunService.Heartbeat:Connect(function()
    if loopFling then
        local target = findTarget(nameBox.Text)
        if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
            local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.CFrame = target.Character.HumanoidRootPart.CFrame + Vector3.new(2, 0, 0)
                startFling()
            end
        else
            stopFling()
        end
    end
end)

-- Noclip logic
RunService.Stepped:Connect(function()
    if noclipEnabled then
        local char = plr.Character
        if char then
            for _, part in pairs(char:GetDescendants()) do
                if part:IsA("BasePart") and part.CanCollide == true then
                    part.CanCollide = false
                end
            end
        end
    end
end)

-- Noclip via E key
UIS.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == Enum.KeyCode.E then
        noclipEnabled = not noclipEnabled
        noclipBtn.Text = noclipEnabled and "🚶‍♂️ NOCLIP: ON" or "🚶‍♂️ NOCLIP: OFF"
        noclipBtn.BackgroundColor3 = noclipEnabled and Color3.fromRGB(80, 30, 120) or Color3.fromRGB(40, 20, 60)
    end
end)

print("RAGE FLING PANEL LOADED")
